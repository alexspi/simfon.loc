{% extends 'admin/adminbase.twig' %}

{% block body %}
    <a href="{{ path('app_logout') }}">Выйти</a>
    <h1>
        {{ title }}
    </h1>
    <a href="{{ path('admin_user') }}">Usveri</a>
{% endblock %}