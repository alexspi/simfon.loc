{% extends 'admin/adminbase.twig' %}

{% block body %}
    <h1>
        {{ title }}
    </h1>
    <a href="{{ path('admin_user_create') }}">Новый пользователь</a>
    <h2>Список пользователей</h2>
    <ul>
        {% for user in users %}
        <li> {{ user.email }} ,{{ user.roles|json_encode}}          </li>
        {% endfor %}
    </ul>

{% endblock %}